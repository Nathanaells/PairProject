const {User, Profile, HealthRecord, HealthRecordActivity, Activity} = require('../models');
const {Op} = require('sequielize');

class Controller{
    static async home(req, res){

    }
}


module.exports = Controller;